attribute inspirations and sources properly, both out of respect and pragmatism: people can and should compare.

- drizzle: studio inspiration, generally very high working-with-databases standard!
- typesql: mostly vendored as the typescript type generation
- schemainspect+migra: inspired by the libraries in pgkit which were python->ts ports of djrobstep's versions
- sql-formatter: the whole lib is essentially vendored in
- pgkit: most of it. written by me but this is sqlite. will likely be postgres too some day

should also probably do an audit of all the docs
