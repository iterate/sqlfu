a skill `npx skills add sqlfu/using-sqlfu` or even `npx skills add sqlfu/db-access`

skill should tell the agent:
- that we're using sqlfu
- about `definitions.sql`, `sql/*.sql`, `migrations/*.sql`
- that migrations should usually not be manually authored
- about the generate command (maybe there should be a tool and/or a hook?)
