packages:
  - packages/*
  - website
  - website/animations

ignoredBuiltDependencies:
  - esbuild
  - workerd

onlyBuiltDependencies:
  - simple-git-hooks
  # Needed by adapter and recipe tests that open real better-sqlite3 databases.
  # Without a built native binding they throw `Could not locate the bindings file`.
  - better-sqlite3
